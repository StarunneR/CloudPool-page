image: node:13.14.0
cache:
  paths:
    - node_modules/

before_script:
  - npm install hexo -g
  - npm install

pages:
  script:
    - hexo generate
  artifacts:
    paths:
      - public
  only:
    - master